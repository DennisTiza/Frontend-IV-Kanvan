## 1. DetalleTarjetaComponent — Corregir condición

- [x] 1.1 Cambiar `input < maxPermitido` a `input <= maxPermitido` en `puedeRegistrarParada()`

## 2. Verificación

- [x] 2.1 Ejecutar `ng build` y verificar que no hay errores de compilación
