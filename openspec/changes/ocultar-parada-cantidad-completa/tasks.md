## 1. DetalleTarjetaComponent — Condición de parada

- [ ] 1.1 Agregar `&& input < (proceso.cantidad ?? 0)` en `puedeRegistrarParada()`

## 2. Verificación

- [ ] 2.1 Ejecutar `ng build` y verificar que no hay errores de compilación
